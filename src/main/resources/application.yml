spring:
  application:
    name: project-backend
  datasource:
    url: jdbc:postgresql://49.51.207.171:5432/postgres
    username: nekro_agent
    password: nekro_agent
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:

        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true
